const { containsText } = require('./containsText');
const { greaterThan } = require('./greaterThan');
const { between } = require('./between');
const { onContinent } = require('./onContinent');
const { orderByColumn } = require('./orderByColumn');
const { citiesOfCountry } = require('./citiesOfCountry');
const { getPopulation } = require('./getPopulation');
const { totalAmount } = require('./totalAmount');

module.exports = {
  containsText,
  greaterThan,
  between,
  onContinent,
  orderByColumn,
  citiesOfCountry,
  getPopulation,
  totalAmount
};
